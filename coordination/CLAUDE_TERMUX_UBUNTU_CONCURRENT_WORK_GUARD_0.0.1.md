# Claude Termux/Ubuntu Concurrent Work Guard — 8x8 OS 0.0.1 Beta

**Observed:** 2026-08-06 08:27 America/New_York

Claude is reportedly performing live updates in the Termux/Ubuntu CLI estate. Therefore, every GitHub-side census, repository classification, version migration, architecture document and status claim created before Claude finishes is a provisional snapshot, not final system truth.

## Required coordination rule

Do not perform destructive cleanup, service replacement, package installation, database migration, runtime restart, canonical-path reassignment, branch force-update or production promotion in parallel with Claude's active local mission unless the active lease, exact scope and rollback plan are known.

## Claude completion handoff requirements

Claude must emit a complete handoff containing:

- mission/task ID;
- coordinator and agent identity;
- start and end timestamps;
- exact host, Termux and Ubuntu context;
- commands executed or script path;
- files created, modified and removed;
- repositories, branches and exact before/after commits;
- packages installed, upgraded or removed;
- services restarted, enabled, disabled or replaced;
- database and schema changes;
- configuration and secret-path changes without exposing secret values;
- active leases and owner approvals used;
- tests and canaries run;
- output and receipt hashes;
- residual processes, temporary files and cleanup state;
- rollback instructions;
- unresolved failures, warnings and blocked work;
- Three-Reality and evidence-state classifications;
- confirmation that the canonical product version remains 0.0.1 Beta.

## Reconciliation rule

After the handoff is available:

1. capture fresh Termux and Ubuntu filesystem, service, package, process, repository and database census;
2. compare against the pre-mission snapshot;
3. reconcile with GitHub exact heads and unpushed local commits;
4. classify every change as Private Past, Protected Beta, Public Present or Future Lab;
5. update the total-estate manifest only from observed evidence;
6. mark conflicting earlier claims as superseded rather than silently overwriting them;
7. do not infer whole-system completion from Claude's mission completion.

## Current interpretation

Recent GitHub health receipts prove that the local health watcher continued publishing through 2026-08-06T11:27:23Z. They do not reveal Claude's current unpushed local changes, live process state, working-tree modifications or final mission outcome.
